spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/demo_spring_concepts
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: 'create-drop'
